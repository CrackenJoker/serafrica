<div class="sidenav" [ngClass]="collapsed?'sidenav-collapsed':''">
    <div class="logo-container">
        <button class="logo" (click)="abrirMenu()">S</button>
        <div class="logo-text" @fadeInOut *ngIf="collapsed">Serafrica</div>
        <button class="btn-close" @rotate *ngIf="collapsed" (click)="fecharMenu()">
            <i class="fal fa-time close-icon x">X</i>
            
        </button>
    </div>
    <ul class="sidenav-menu">
        <li class="sidenav-menu-item" *ngFor="let dados of navData">
            @if (dados.routerLink == 'dashboard'){
                <!--div ngbDropdown class="d-inline-block">
                    <a 
                    [routerLink]="[dados.routerLink]" 
                    class="sidenav-menu-link" routerLinkActive="active" 
                    [routerLinkActiveOptions]="{exact:true}">
                
                        <i class="sidenav-link-icon" [class]="dados.icon"></i>
                        <button type="button" class="btn btn-outline-primary sidenav-link-text" @fadeInOut *ngIf="collapsed">
                            {{dados.label}}
                        </button>
                    </a>
                </div-->
            }
                <div ngbDropdown class="d-inline-block">
                    @if ((dados.routerLink=='dashboard') && (dados.icon=='fal fa-home')&& (dados.label=='Dashboard')){
                            <a 
                            [routerLink]="[dados.routerLink]" 
                            class="sidenav-menu-link" routerLinkActive="active" 
                            [routerLinkActiveOptions]="{exact:true}">
                        <!--btn-outline-primary-->
                                <i class="sidenav-link-icon" [class]="dados.icon"></i>
                                <button type="button" class="btn btn-outline-primary sidenav-link-text" @fadeInOut *ngIf="collapsed">
                                    {{dados.label}}
                                </button>
                            </a>
                        
                    }@else{
                        <a 
                    [routerLink]="[dados.routerLink]" 
                    class="sidenav-menu-link" routerLinkActive="active" 
                    [routerLinkActiveOptions]="{exact:true}">
                <!--btn-outline-primary-->
                        <i class="sidenav-link-icon" [class]="dados.icon"></i>
                        <button type="button" class="btn btn-outline-primary sidenav-link-text" id="dropdownBasic1" ngbDropdownToggle
                        @fadeInOut *ngIf="collapsed"
                        >
                            {{dados.label}}
                        </button>
                        <div ngbDropdownMenu aria-labelledby="dropdownBasic1">
                            <button ngbDropdownItem>Action - 1</button>
                            <button ngbDropdownItem>Another Action</button>
                            <button ngbDropdownItem>Something else is here</button>
                        </div>
                    </a>
                    }
                    
                </div>
            

            
       
          </li>
               
            
        
    </ul>
</div>
